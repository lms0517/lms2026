# KPP 영업마스터 — 작업 인수인계 문서 (GPT용)

> 이 문서 하나만 읽으면 이 프로젝트를 처음 보는 AI/사람도 바로 유지보수를 이어갈 수 있도록 작성되었습니다.
> ChatGPT에서 이어서 작업할 때는 **이 문서 + `index.html`** 두 개를 함께 업로드하세요.

---

## 0. GPT에게 먼저 시킬 말 (복사해서 첫 메시지로 붙여넣기)

```
너는 지금부터 "KPP 영업마스터" 모바일 웹앱의 유지보수 개발자다.
첨부한 HANDOFF-GPT.md(프로젝트 인수인계 문서)와 index.html(전체 소스, 단일 파일)을 먼저 정독해라.

작업 규칙:
1. 이 앱은 빌드 도구·프레임워크·서버가 전혀 없는 순수 단일 HTML 파일이다.
   외부 라이브러리 추가 금지(QR 라이브러리 1개만 예외, CDN 동적 로드).
   React/Vue/Tailwind/npm 등을 절대 도입하지 마라.
2. 수정 요청을 받으면 index.html 전체를 다시 뱉지 말고,
   "몇 번째 줄 근처의 어떤 함수/블록을 어떻게 바꿔라"는 형태로
   변경 전/후 코드 조각(diff)만 제시해라.
3. 기존 코드 스타일을 그대로 따라라 — 한국어 주석, 짧은 변수명(k, b, n, s, u),
   화살표 함수, 템플릿 리터럴로 HTML 문자열 생성, `$()` 셀렉터 헬퍼.
4. 모든 데이터는 localStorage에 저장된다(`kpp3.` 접두사). 서버·DB 없다.
   저장 키를 바꾸면 사용자의 기존 데이터가 날아가니 절대 임의로 바꾸지 마라.
5. 디자인 토큰(:root CSS 변수, 네이비+골드)을 벗어나는 색을 새로 만들지 마라.
6. 모바일 세로 화면(최대 폭 520px) 기준으로만 설계한다. 데스크톱 레이아웃은 고려 대상이 아니다.

먼저 파일을 다 읽었다는 확인과 함께, 현재 구조를 3줄로 요약해서 보고해라.
```

---

## 1. 프로젝트 한 줄 요약

한국파렛트풀(KPP) 영업 담당자가 **현장에서 스마트폰으로** 쓰는 영업 지원 웹앱.
사업 설명자료 전달 · 영업 스킬 확인 · 견적 전 체크리스트 · 즉석 견적 개산 · 주변 업체 관리 · 사내 지식 검색을 한 앱에서 처리한다.

- **저장소**: `github.com/lms0517/lms2026`
- **배포**: GitHub Pages (저장소 루트 서빙 → `index.html`이 곧 앱)
- **작업 브랜치**: `claude/migrate-work-to-gpt-pqewzj`
- **디자인 컨셉**: 시안 C — 네이비 프리미엄 (`#0a2d61` 네이비 + `#f5c344` 골드)

---

## 2. 파일 구성 (전부 4개)

| 파일 | 크기 | 역할 |
|---|---|---|
| `index.html` | 약 136KB / 950줄 | **앱 전체.** HTML + CSS + JS + 데이터가 모두 이 한 파일 안에 있다 |
| `README.md` | 2KB | 저장소 설명 |
| `A_professional_hero_image_...png` | 762KB | 히어로 이미지 소재 (현재 코드에서 참조하지 않음, 보관용) |
| `HANDOFF-GPT.md` | 이 문서 | 인수인계 문서 |

> **중요**: 빌드 단계가 없다. `index.html`을 브라우저로 열면 그대로 동작한다.
> `npm install`, 번들러, 트랜스파일러가 존재하지 않으며 앞으로도 도입하지 않는 것이 이 프로젝트의 설계 원칙이다.

---

## 3. 화면 구조

하단 고정 탭 3개로 나뉜다.

```
┌─ 사업 (#/)  ────────────────────────────────────┐
│  사업분야 6개 카드 그리드                          │
│   PPS · SCM · 산업자재유통 · 글로벌비즈            │
│   · 물류장비 · 자동화솔루션                        │
│                                                  │
│   각 사업 진입 (#/biz/{키}) → 카테고리 4개         │
│    1) 사업소개서  — 카톡/문자 메시지 복사·공유,     │
│                    즉석 QR 생성, A4 인쇄용 QR      │
│    2) 핵심 영업스킬 (#/biz/{키}/skill)            │
│                  — 한 줄 피치 / 영업 포인트 /      │
│                    반론(클레임) 대응               │
│    3) 체크리스트 (#/biz/{키}/check)               │
│                  — 견적 전 필수 확인, 체크 후      │
│                    견적요청 양식 자동 생성·복사     │
│    4) 견적산출 (#/biz/{키}/quote)                 │
│                  — 사업별 전용 계산기 + 단가 참고   │
│                    + 외부 전문 견적앱 링크         │
├─ 업체 (#/corp) ─────────────────────────────────┤
│  내 주변 영업대상업체                              │
│   · 내부데이터: 등록 업체를 현재 위치 기준 거리순    │
│   · 주변검색: 네이버 지도 키워드 검색 링크          │
├─ 지식 (#/know) ─────────────────────────────────┤
│  회사소개 · 영업 기본 · 교육자료                   │
│   검색 / 분류 필터 / 직접 추가                     │
└──────────────────────────────────────────────────┘
```

### 라우팅
해시 기반 SPA. `render()` 함수(약 332줄)가 `location.hash`를 파싱해 화면을 그린다.

| 해시 | 화면 | 함수 |
|---|---|---|
| `#/` | 홈 (사업분야 6개) | `vHome()` |
| `#/biz/{k}` | 사업 상세 (카테고리 4개) | `vBiz(b)` |
| `#/biz/{k}/skill` | 영업스킬 | `vSkill(b)` |
| `#/biz/{k}/check` | 체크리스트 | `vCheck(b)` |
| `#/biz/{k}/quote` | 견적산출 | `vQuote(b)` |
| `#/corp` | 업체 | `vCorp()` |
| `#/know` | 지식 | `vKnow()` |

사업 키(`k`): `pps` `scm` `ind` `glo` `mhe` `ces`

---

## 4. index.html 코드맵 (줄 번호는 대략치 — 수정하면 밀린다)

### 4-1. `<head>` — 1~12줄
메타 태그, PWA 매니페스트(data URI 인라인), 파비콘(SVG data URI 인라인).
외부 파일 참조가 하나도 없도록 전부 data URI로 인라인 처리되어 있다.

### 4-2. `<style>` — 13~197줄
```
13   :root 디자인 토큰 (색상·반경·그림자)
39   헤더
48   본문 공통 (.body .sec-t .card .hint)
57   메인: 사업분야 그리드 (.bizgrid .bizc)
66   사업 상세
85   콘텐츠 페이지 (스킬/체크)
104  버튼
113  견적 계산기
134  업체
155  지식
170  하단 탭
179  시트/모달
```

**디자인 토큰 (건드리지 말 것 — 새 색을 추가하지 말고 여기서 골라 쓸 것)**
```css
--navy:#0b3a7c   --navy-d:#0a2d61   --navy-x:#082448
--gold:#f5c344   --gold-d:#d9a51f   --gold-bg:#f9f1da
--bg:#eef1f6     --card:#ffffff     --line:#d9dfeb  --line2:#e4e9f2
--txt:#141d33    --sub:#4d5e78      --mut:#8b99ad
--ok:#1e7a46 / --ok-bg:#e6f4ec      --warn:#b3341f / --warn-bg:#faeae6
--r:10px         --sh:0 2px 10px rgba(10,25,55,.07)
```
레이아웃: `#shell { max-width:520px }` — 모바일 세로 전용.

### 4-3. `<body>` — 199~206줄
DOM은 껍데기 5개뿐. 나머지는 전부 JS가 문자열로 생성해 주입한다.
```html
<div id="shell">
  <div id="view"></div>     <!-- 화면 본문이 여기 통째로 교체됨 -->
  <nav id="tabbar"></nav>   <!-- 하단 탭 -->
</div>
<div id="sheetbg"></div>    <!-- 바텀시트 배경 -->
<div id="sheet"></div>      <!-- 바텀시트 본문 -->
<div id="toast"></div>      <!-- 토스트 알림 -->
```

### 4-4. `<script>` — 208~948줄

**데이터 상수 블록 (여기가 콘텐츠 수정 지점)**
| 줄 | 상수 | 내용 |
|---|---|---|
| 215 | `I` | SVG 아이콘 모음 |
| 240 | `BIZ` | 사업분야 6개 정의 (키·이름·설명·아이콘) |
| 251~254 | `TK_BASE` `BIZ2TK` `introUrl` `introQrPage` | 사업소개서 딥링크 (외부 X2026 툴킷) |
| 257 | `SKILL` | 영업스킬 본문 — `{사업키: [[제목, 본문], ...]}` |
| 275 | `CHECK` | 체크리스트 항목 |
| 285 | `PRICE_REF` | 견적 페이지 하단 단가 참고 원문 |
| 288 | `PRO_APPS` | 외부 전문 견적앱 링크 (scm/glo/ces) |
| 526 | `QUOTE_SUB` | 견적 페이지 부제 |
| 824 | `CORP_SEED` | 업체 초기 데이터 3건 |
| 839 | `NAVER_KW` | 주변검색 키워드 |
| 908 | `KNOW_SEED` | 지식 초기 데이터 5건 |

**유틸 (297~318줄)**
```js
$(s)          // document.querySelector 축약
won(n)        // 숫자 → 천단위 콤마 원화 표기
esc(s)        // HTML 이스케이프 (사용자 입력 주입 시 필수)
store.get/set // localStorage 래퍼 — 키에 'kpp3.' 접두사 자동
toast(m)      // 하단 토스트
copyText(s)   // 클립보드 복사 (fallbackCopy 폴백 있음)
openSheet(html) / closeSheet()  // 바텀시트
```

**라우터·화면 (319~362줄~)**
`nav(h)` → 해시 변경 → `render()` → 해당 `v___()` 함수가 `#view.innerHTML`을 통째로 교체.
`hdr(o)` (350줄) 는 공통 헤더 HTML을 만든다.

**사업소개서 / QR (404~449줄)**
```
introMsg(k)     카톡·문자 전달용 메시지 문안 생성
shIntroSend(k)  전달 시트 (복사 / 공유)
shIntroQR(k)    QR 시트
makeQR(u)       QR 생성 — CDN 라이브러리를 필요할 때만 동적 로드
qrFallback(u)   라이브러리 로드 실패 시 대체 표시
```

**체크리스트 (467~525줄)**
```
checkModel(k)  체크 항목 모델
chkToggle      체크 상태를 store('chk.{사업키}')에 저장
chkProg(k)     진행률 표시
chkCopy(k)     체크 결과 → 견적요청 양식 텍스트로 복사
```

**견적 계산기 (526~823줄) — 사업별 1개씩**
| 함수 | 줄 | 계산 내용 |
|---|---|---|
| `qPPS()` | 553 | 파렛트 렌탈료 vs 자가 보유 비교 |
| `qSCM()` | 607 | 운송·보관 개산 |
| `qIND()` | 659 | 산업자재 3종 — 스트레치필름 / 골판지 / 아이스팩 |
| `qGLO()` | 722 | RRPP 편도 vs 회수 비교 |
| `qMHE()` | 754 | 지게차 임대 |
| `qCES()` | 791 | 자동화설비 개산 |

**단가는 사용자가 화면에서 수정 가능하고, 수정값은 기기에 저장된다.**
```js
rateGet(k, d)   // 기본 단가 d 위에 저장된 사용자 단가를 덮어씀
rateSave(k, o)  // store('rate.{사업키}')에 저장
```
→ 새 계산기를 추가하거나 기본 단가를 바꿀 때 반드시 이 두 함수를 통해서 처리할 것.

**업체 (824~907줄)**
```
corpList()/corpSave()  localStorage 'corps'
distKm(a,b)            두 좌표 간 거리 (하버사인)
corpLocate()           navigator.geolocation으로 현재 위치 취득 → store('pos')
corpSetMode(m)         내부데이터 / 주변검색 전환
corpAddSheet/corpAdd/corpDel   업체 추가·삭제
```

**지식 (908~947줄)**
```
knowList()      localStorage 'know'
vKnow()         검색어(knowQ) + 분류(knowCat) 필터링 렌더
knowAddSheet/knowAdd
```

---

## 5. 데이터 저장 — localStorage 전부

서버도 DB도 없다. 모든 상태가 사용자 브라우저에만 남는다.
`store` 래퍼가 모든 키에 **`kpp3.`** 접두사를 자동으로 붙인다.

| 실제 저장 키 | 내용 |
|---|---|
| `kpp3.chk.{사업키}` | 체크리스트 체크 상태 |
| `kpp3.rate.{사업키}` | 사용자가 수정한 견적 단가 |
| `kpp3.corps` | 등록 업체 목록 |
| `kpp3.pos` | 마지막으로 취득한 내 위치 좌표 |
| `kpp3.corpMode` | 업체 탭 모드 (`all` / 주변검색) |
| `kpp3.know` | 지식 항목 목록 |

**주의**: 저장 키 이름을 바꾸면 현장 사용자들이 그동안 입력한 업체·단가·지식이 전부 사라진다.
스키마를 바꿔야 한다면 반드시 마이그레이션 코드를 함께 넣을 것.

---

## 6. 외부 의존성 (딱 4가지, 전부 링크)

| 대상 | URL | 용도 |
|---|---|---|
| X2026 툴킷 | `https://lms0517.github.io/X2026/toolkit/?biz={코드}` | 사업소개서 딥링크 (고객에게 전달) |
| 운송견적 전문앱 | `https://ipgomi-app.github.io/a2026/` | SCM 견적 페이지에서 링크 |
| RRPP 견적앱 | `https://lms0517.github.io/X2026/rrpp/` | 글로벌비즈 견적 페이지에서 링크 |
| 엔지니어링 견적앱 | `https://ipgomi-app.github.io/B2026/v2/` | CES 견적 페이지에서 링크 |

사업키 → 툴킷 코드 매핑 (`BIZ2TK`, 252줄):
```js
pps→pool   scm→sc   glo→global   ind→supply   mhe→mhe   ces→ces
```

그 외에 QR 생성 라이브러리 1개를 CDN에서 **필요할 때만 동적 로드**한다(`makeQR`, 431줄). 실패 시 `qrFallback`으로 링크 표시로 대체된다.

---

## 7. 자주 하게 될 유지보수 작업 레시피

### 영업스킬 문구 수정
→ `SKILL` 상수(257줄)에서 해당 사업키의 `[제목, 본문]` 배열을 고친다. 그게 전부다.

### 체크리스트 항목 추가
→ `CHECK` 상수(275줄)에 항목 추가. 체크 상태 키는 인덱스 기반이므로 **중간에 삽입하면 기존 체크 상태가 밀린다.** 되도록 뒤에 추가할 것.

### 견적 단가 기본값 변경
→ 해당 `q___()` 함수 안의 `rateGet(k, {기본값객체})` 부분을 수정. 이미 단가를 수정해 쓰던 사용자에게는 저장값이 우선하므로 반영되지 않는다는 점에 유의.

### 사업분야 추가
→ ① `BIZ`에 항목 추가 ② `SKILL`/`CHECK`/`QUOTE_SUB`/`PRICE_REF`에 같은 키로 콘텐츠 추가 ③ `q___()` 계산기 함수 작성 ④ `vQuote()`의 분기에 연결 ⑤ 소개서 링크가 필요하면 `BIZ2TK`에 매핑 추가.

### 배포
→ `index.html`을 커밋해서 기본 브랜치에 올리면 GitHub Pages가 자동 반영. 별도 빌드/배포 명령 없음.

---

## 8. 코딩 스타일 (기존 코드와 톤을 맞출 것)

- 주석은 **한국어**, `/* ---------- 구분선 ---------- */` 형태로 섹션을 나눈다.
- 변수명은 짧게: `k`(사업키) `b`(사업객체) `n`(이름) `s`(설명) `u`(URL) `t`(제목)
- 화면은 **템플릿 리터럴로 HTML 문자열을 만들어 `innerHTML`에 주입**한다. DOM API로 하나씩 만들지 않는다.
- 이벤트는 대부분 인라인 `onclick="함수()"` — 전역 함수로 선언해야 동작한다.
- 사용자 입력을 화면에 넣을 때는 **반드시 `esc()`** 를 통과시킨다.
- 세미콜론 사용, 들여쓰기 1~2칸 혼용(기존 파일 그대로 따라감).

---

## 9. 작업 이력

| 커밋 | 내용 |
|---|---|
| `17b14c8` | KPP 영업마스터 재설계: 사업·업체·지식 3탭 구조 (시안 C 네이비 프리미엄) |
| `dff85ef` | Add files via upload |

---

## 10. 알아둘 제약 / 주의사항

1. **서버가 없다** — 업체·지식 데이터를 여러 사람이 공유할 방법이 현재는 없다. 공유가 필요해지면 그때는 백엔드 도입 여부를 사용자와 먼저 논의해야 하는 구조 변경이다.
2. **위치 기능은 HTTPS에서만 동작** — `navigator.geolocation`은 로컬 파일(`file://`)로 열면 막힌다. 테스트는 GitHub Pages나 로컬 HTTP 서버로 할 것.
3. **클립보드 복사도 HTTPS 필요** — `navigator.clipboard` 실패 시 `fallbackCopy()`가 받아준다.
4. **단가는 전부 참고용** — 코드에 들어있는 금액은 확정 단가가 아니다. UI에도 그렇게 명시되어 있으니 이 문구를 임의로 지우지 말 것.
5. **파일 하나가 136KB** — GPT에 업로드해서 전체를 한 번에 다루기엔 큰 편이다. 수정 요청은 "어느 함수"인지 지정해서 부분 수정으로 진행하는 편이 안전하다.
