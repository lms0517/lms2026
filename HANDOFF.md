# 인계 메모

이 브랜치(`claude/sales-hub`)에서 이어서 작업합니다.
직전 세션에서 흩어진 저장소 세 곳을 여기로 모았습니다.

## 지금 상태

- 대시보드 · RRPP 견적앱 · 장비 견적앱 · 소개서 툴킷 · 파렛트 카탈로그가 한 브랜치에 있습니다
- 앱 주소는 전부 `https://lms0517.github.io/lms2026/…` 기준으로 맞춰져 있습니다
- 용어는 원래대로 복원되어 있습니다 (파렛트 · KPP · 로지스올 · 물류 · WMS/WCS/TMS · PPS/SCM/RRPP/CES/MHE)
- 기존 브랜치는 건드리지 않았습니다

## 먼저 해야 할 일

### 1. Pages 소스를 이 브랜치로 바꾸기

지금 Pages는 `claude/forklift-quote-app-7vjj9b` 를 보고 있어서 `lms0517.github.io/lms2026/` 에는 옛 화면이 뜹니다.

`Settings → Pages → Branch` 를 **`claude/sales-hub` / `(root)`** 로 바꾸면 대시보드가 바로 뜹니다.
(`index.html` 은 대시보드로 보내는 리다이렉트입니다. 앱 모음은 `hub.html` 에 있습니다.)
바꾸면 `lms0517.github.io/lms2026/equip.html` 주소는 없어지고 `…/apps/equip/equip.html` 이 됩니다.
기존 주소를 살려야 하면 루트에 리다이렉트 파일을 하나 두면 됩니다.

### 2. 견적앱 원가표 암호화 (대기 중)

`대시보드.html` 안 PPS 견적앱의 `DATA` 객체가 **평문**입니다. 사내 원가표라 공개 저장소에 그대로 두면 안 됩니다.

방식은 업무일지와 같습니다 — 비밀번호도 같은 것을 씁니다.

```
PBKDF2-SHA256 150,000회 → AES-256-GCM
base64( salt(16) + iv(12) + 암호문 )
```

절차:

1. 아래 형태의 오프라인 도구를 만들어 사용자에게 파일로 전달합니다. 브라우저에서 열어 비밀번호를 넣으면 암호문이 나옵니다. **비밀번호를 대화나 커밋에 남기지 않는 것이 핵심입니다.**
2. 사용자가 돌려준 암호문을 `ENC_COST` 상수로 넣고 평문 `DATA` 블록을 지웁니다.
3. 계산기 래퍼를 `(function(){ … })();` 에서 `window.__ppsCalcInit=function(DATA){ … };` 로 바꿔 잠금해제 후에만 실행되게 합니다. **계산식은 건드리지 않습니다.**
4. 견적앱 화면(`v-ppsq`)에 업무일지와 같은 모양의 🔒 잠금 카드를 붙이고, 비밀번호가 같으므로 한쪽을 풀면 양쪽이 열리게 합니다.
5. 검증 — 잠긴 상태에서 `DATA` 접근 불가, 틀린 비밀번호 거부, 해제 후 산출값이 종전과 동일한지 다음 조합으로 확인합니다.

```
N11/B/60일   원가 14.999 | 2,156 | 406
N11/A+/30일  원가 24.270 | 1,913 | 406
N12/A/45일   원가 12.946 | 2,041 | 499
P13/C/120일  원가  0.728 | 2,499 | 658
P13/B/90일   원가 11.992 | 3,307 | 658
```

이 절차는 직전 세션에서 시험 비밀번호로 전 과정을 검증해 두었습니다.

### 3. SCM 견적앱 (자료 대기)

`계약물류사업` 의 핵심 영업 스킬과 견적 필수 확인은 사내 자료 기준으로 작성되어 있습니다.
견적앱을 만들려면 다음 다섯 가지가 더 필요합니다. RFP 양식에 빈칸이거나 "시트참조"로만 되어 있습니다.

- 작업비(원/Box) 산출 기준
- 택배 단가 시트
- 용차 구간별 단가표
- 부자재 원가표
- 평 ↔ PLT 환산 확정 계수 (지금은 RFP 예시에서 역산한 1평당 1.5~1.7 PLT)

견적 구조는 이미 정해져 있습니다.

```
보관비 = 면적(평) × 단가        고정비
작업비 = 출고 Box 수 × 단가      재고보관 · 전산운영 · 입출고 전반 포함
택배비 = Box 호수별 수량 × 단가
```

## 알아두면 좋은 것

### 업무일지 암호문을 조심할 것

`ENC_LOG` 상수 안에 `S1`~`S5` 같은 문자열이 **우연히 16번** 들어 있습니다.
용어를 일괄 치환할 때 이 줄을 제외하지 않으면 암호문이 깨져 업무일지를 영영 못 엽니다.
코드 식별자(`S1_SEC`, `S2_SEC` 등)도 같은 이유로 건드리면 안 됩니다.

### 카카오 지도

주변 업체 화면이 카카오 지도를 씁니다. JS 키는 도메인으로 보호되며 `lms0517.github.io` 와 `ipgomi-app.github.io` 가 등록되어 있습니다.
등록되지 않은 도메인(미리보기 등)에서는 설계대로 개략도로 대체됩니다.

### 앱을 늘리는 법

`대시보드.html` 의 `TOOLS` 배열에 한 줄만 넣으면 카테고리 카드와 상세 목록이 자동으로 따라옵니다.

- `type:'app'` + `url` — 새 창
- `type:'app'` + `view` — 대시보드 안 화면 (PPS 견적앱이 이 방식)
- `type:'calc'` + `el` — 정적 계산기 노드를 상세 화면으로 옮겨 붙임

### 사업 아이콘

`icons/<key>.png`(jpg·webp도 됨)를 넣으면 이모지 대신 그 그림이 뜹니다. 없으면 이모지를 그대로 씁니다.
키는 `s1`(PPS) · `s2`(SCM) · `glo` · `ind` · `s5`(MHE) · `s4`(CES) 입니다.

### 아직 외부에 있는 앱

옮기면 이미 공유된 링크가 깨져서 그대로 두었습니다.

- 운송견적 — `ipgomi-app.github.io/a2026/`
- 엔지니어링 제안 — `ipgomi-app.github.io/B2026/v2/`

### 미확인 상태로 남겨둔 것

- 파렛트 카탈로그 각 면에 KPP 로고와 고객사 실명(오뚜기 · LG전자 등)이 있습니다. 홍보 자료라 그대로 두기로 했습니다
- 원본 PDF 12MB 도 그대로 둡니다
- `NF-48형` → 코스트코 전용, `N11T형` → 트레이더스 전용 으로 되돌렸습니다. 규격으로 판단한 것이라 뒤바뀌었으면 고쳐야 합니다

## 작업 방식

한 번 바꿀 때마다 이렇게 확인해 왔습니다.

1. `<script>` 블록을 뽑아 `node --check`
2. Playwright + Chromium(`/opt/pw-browsers/chromium`)으로 390×844 모바일 뷰포트에서 실행, `pageerror` 수집
3. 새 화면을 붙였으면 **스크롤이 끝까지 닿는지** 확인 (뷰는 반드시 `#main` 안에 넣을 것. 밖에 두면 `body` 가 `overflow:hidden` 이라 아래가 잘린다)
4. 견적앱을 건드렸으면 원가 5개 조합이 종전과 같은지 대조
